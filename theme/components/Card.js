import { defineStyleConfig } from "@chakra-ui/react";

export const Card = defineStyleConfig({
  baseStyle: {
    container: {
      borderRadius: 0,
    }
  },
  variants: {
    outline: {
      
    }
  }
})